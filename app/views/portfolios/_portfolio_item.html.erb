<div class="card" data-id="<%= portfolio_item.id %>">
  <%= image_tag portfolio_img(portfolio_item.thumb_image, 'thumb') %>
  <p class="card-text">
    <span><%= link_to portfolio_item.title, show_portfolio_path(portfolio_item) %></span> <%= portfolio_item.subtitle %>
  </p>
</div>
